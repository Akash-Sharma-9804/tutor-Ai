export const AUTOPLAY_PAUSE_SECONDS = 5;      // seconds to wait between segments in autoplay
export const MAX_TTS_CHUNK_SIZE = 1500;        // chars — max text length sent to Deepgram at once
export const AUTOPLAY_START_DELAY_MS = 400;    // ms delay before starting read after segment change
export const SUBHEADING_SKIP_DELAY_MS = 100;   // ms before auto-skipping a subheading segment
export const FULLSCREEN_ENTER_DELAY_MS = 500;  // ms after load before attempting fullscreen
export const TTS_WORD_PACE_ESTIMATE = 0.38;    // seconds per word for estimated audio duration
export const EQUATION_STEP_REVEAL_INTERVAL_MS = 2600; // ms between equation step reveals
export const EQUATION_STEP_TYPING_DURATION_MS = 3000; // ms to type a single step